## Secret handling

Use env vars or secure config management for sensitive data. Never put literal secrets in code.

## Data exfiltration

## Defense in depth

## Data redaction/sanitization

## Boundaries

End-user clients (a web browser) cannot be trusted.
Validate client inputs.

## General

Look for security vulnerabilities so that we can patch them.
